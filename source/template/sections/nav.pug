nav.nav
  - var items = ['О компании', 'Продукция', 'Благотворительность', 'Пресс-центр', 'кулинарный центр', 'Контакты']
  - var dataItems = ['header', 'about', 'approch', 'work', 'approchs', 'works']
  
  each item, key in items
    if item === 'О компании'
      a.nav__link.nav__link--active(href="#" data-link=dataItems[key])= item
    else
      a.nav__link(href="#" data-link=dataItems[key])= item
  
include languages


  
  

